apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oxia-proof.fullname" . }}-test-connection"
  labels:
    {{- include "oxia-proof.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oxia-proof.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
